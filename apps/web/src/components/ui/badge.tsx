import React from 'react';

export const Badge = ({ children, className }: any) => <span className={className}>{children}</span>;

// For default import compatibility
const Stub = () => null;
export default Stub; 